import XCTest

final class NativePipeRemoteAppTests: XCTestCase {
    func testPlaceholder() throws {
        // Remote/SSH feature tests were intentionally reverted from the working
        // set for this run. This placeholder keeps SwiftPM targets valid.
        XCTAssertTrue(true)
    }
}

